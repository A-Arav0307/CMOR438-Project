# PCA

Principal component analysis on the housing features (target dropped).

Full decomposition with the variance ratio plot, then 2D and 3D projections, the component loadings to see which original features each PC pulls from, reconstruction error vs number of components kept, and a from-scratch PCA via the SVD that mirrors `src/rice_ml/unsupervised_ml/pca.py`.
