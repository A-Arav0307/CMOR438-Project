# Unsupervised Machine Learning

California housing dataset. Target dropped; features only.

- **1. PCA** — variance ratio, 2D/3D projection, component loadings, reconstruction error, from-scratch
- **2. K-Means** — elbow, silhouette, PCA + geographic views, target-vs-cluster, from-scratch
- **3. DBSCAN** — k-distance plot, eps / min_samples sweeps, PCA + geographic views
