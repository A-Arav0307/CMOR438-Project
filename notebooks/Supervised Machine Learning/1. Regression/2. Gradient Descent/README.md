# Gradient Descent

Same housing regression target, but here the focus is the optimizer itself.

A learning rate sweep to see where things blow up vs converge slowly, then batch GD against SGD against mini-batch on the same problem. Ends with test predictions and an error histogram.
