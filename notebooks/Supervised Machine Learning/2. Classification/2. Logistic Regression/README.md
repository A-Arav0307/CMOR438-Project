# Logistic Regression

Same above-median classification task as the perceptron notebook, but with a probabilistic model.

sklearn baseline, regularization sweep over C, then a from-scratch GD implementation. After that an ROC curve with a threshold sweep and a look at the top coefficients to see which features actually push the prediction up or down.
