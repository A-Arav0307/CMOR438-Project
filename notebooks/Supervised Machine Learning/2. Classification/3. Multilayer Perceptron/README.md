# Multilayer Perceptron

Same above-median target, now with hidden layers.

sklearn `MLPClassifier` baseline, then sweeps over architecture (number/size of hidden layers) and activation function. After that a from-scratch one-hidden-layer net with backprop, finishing on an ROC curve.
