# Regression Tree

Decision tree regressor on `median_house_value`.

Baseline first, then a depth sweep to see where overfitting starts, a comparison of splitting criteria (squared error vs friedman_mse vs absolute_error vs poisson), feature importances, predicted-vs-actual, and a shallow tree plot at the end so you can actually read the splits.
