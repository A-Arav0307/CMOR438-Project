# Decision Tree

Classification version: `median_house_value > median`.

Baseline, a depth sweep to find where the tree stops generalizing, splitting criterion comparison (gini vs entropy vs log_loss), feature importances, and a plotted tree with depth limited so it's actually readable.

Last cell runs my from-scratch tree from `rice_ml` on a subsampled training set as a sanity check.
