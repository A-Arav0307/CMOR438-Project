# Perceptron

Binary classification: is a block's `median_house_value` above the overall median?

sklearn baseline, learning rate sweep, then a from-scratch implementation (the same one that lives in `src/rice_ml/supervised_ml/perceptron.py`). Final cell plots the decision boundary on two features so you can see what the model actually learned.
