# K-Nearest Neighbors

KNN on the above-median classification target.

Baseline, k sweep, distance metric and weighting comparison (uniform vs distance, euclidean vs manhattan vs minkowski), then a from-scratch KNN that matches `src/rice_ml/supervised_ml/knn.py`. Decision boundary on two features and an ROC curve to wrap up.
